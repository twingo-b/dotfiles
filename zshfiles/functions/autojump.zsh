# autojump
# http://unix-like.dyndns-web.com/?p=164
# http://blog.dealforest.net/2012/03/homebrew-install-autojump/
if [ -f `brew --prefix`/etc/autojump.sh ]; then
	. `brew --prefix`/etc/autojump.sh
fi
